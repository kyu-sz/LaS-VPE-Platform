apply plugin: 'java'

repositories {
	flatDir(dirs: "$projectDir/libs")
}

dependencies {
	compile group: '', name:'kafka-clients', version: '0.10.0.0'
	compile group: '', name:'spark-assembly', version: '1.6.1-hadoop2.6.0'
	compile group: '', name:'spark-examples', version: '1.6.1-hadoop2.6.0'
}